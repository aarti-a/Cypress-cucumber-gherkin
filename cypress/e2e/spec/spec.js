import { Given, Then, When } from 'cypress-cucumber-preprocessor/steps';

Given('Given user have access to SB-application', () => {
  // do nothing
});

Then('Then user should be able to login the SB-application', () => {
  //do nothing
});


